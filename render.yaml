services:
  - type: web
    name: rrmotors
    buildCommand: npm install && npx vite build && cd server && npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.14.0
      - key: VITE_API_URL
        value: /api
